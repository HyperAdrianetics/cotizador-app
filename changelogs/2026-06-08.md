# Changelog — 2026-06-08

## Cambios generales
- Renombrado el proyecto de `cotizador-app` a `hypernetics-intranet`

## Vercel
- Agregado `rewrites` en `vercel.json` para SPA routing (todas las rutas no-`/api/*` → `index.html`)

## Tema / Colores
- Nuevos colores base en `src/index.css`: `primary-green`, `primary-yellow`, `background`, `primary-blue`, `secondary-blue`
- `primary-green` (#a7cf9e) usado como color principal de acento en toda la app

## Página principal (`Home.tsx`)
- Tarjeta "Branding Mood Board" corregida para usar `primary-green` en hover (igual que las otras)
- Nueva tarjeta "Firma Digital" con acceso a `/firma`
- Grid de 2 a 3 columnas para acomodar la nueva tarjeta

## MoodBoard (`MoodBoard.tsx`)
- Paleta de colores actualizada con los 5 nuevos colores base
- Click en cada círculo copia el hex al portapapeles
- Toast animado al copiar
- Botón "Volver al Inicio" movido a la parte inferior
- Logo de Hypernetics ahora redirige al inicio (`<Link to="/">`)

## Generador de Firmas Digitales (`EmailSignature.tsx`)
- Nueva página en `/firma` con el diseño de firma email (tabla con gradientes)
- Formulario editable: nombre, puesto, teléfono, prefijo de correo
- Dominio `@hypernetics.com.mx` fijo como sufijo
- Botón "Copiar firma digital" copia como HTML enriquecido (compatible con Gmail, Outlook, etc.)
- Fondo unificado con el resto de la app (`from-background via-primary-blue to-background`)
- Botón flotante "Volver al Inicio" en esquina inferior izquierda
- Logo en la firma ahora ocupa el 100% del ancho

## Cotizador (`QuoteEditor.tsx`)
- Botón "Inicio" flotante debajo del indicador de estado de guardado

## Sidebar (`Sidebar.tsx`)
- Sección "Plantillas Hypernetics" ahora es un dropdown colapsable (ícono chevron)

## EmailSignature
- Teléfono sin restricciones (regex eliminado)
- Nombre con `text-overflow: ellipsis` en la firma
